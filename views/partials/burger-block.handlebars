<li>
	{{burger_name}}

	<button class="change-devoured" data-id="{{id}}" data-newsleep="{{devoured}}">
		{{!-- {{#if devoured}}Click to devour{{else}}Devoured{{/if}} --}}
		{{#if devoured}}Click to devour{{/if}}
	</button>
</li>
